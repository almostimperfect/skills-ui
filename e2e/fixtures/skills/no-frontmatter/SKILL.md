# No Frontmatter Skill

This SKILL.md deliberately has no YAML frontmatter. skills-ui must fall back to the directory name
as the skill name and an empty description (src/core/metadata.ts fallback path).
